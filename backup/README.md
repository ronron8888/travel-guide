# Backup / Safe design refactor

The `main` branch remains the production baseline. The `develop` branch is used for the scroll-animation refactor.

This branch adds `style.css` and `script.js` for the new scroll reveal behavior without changing `data.js`.
